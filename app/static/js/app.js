/* PosterPilot - Minimal global JS */

// Add any global utilities here if needed.
// Most interactivity is handled by Alpine.js in templates.

document.addEventListener('DOMContentLoaded', () => {
    console.log('PosterPilot loaded');
});
